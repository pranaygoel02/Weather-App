import React,{useEffect, useState} from 'react'
import axios from 'axios'
export default function Location ({coords}) {
  const pos = {}
  console.log(coords.lat);
  // console.log(coords);
  // console.log(lat);
  // console.log(url);
  // axios.get(url).then(res=>res.data).then(data=>{
  //       console.log('user position: ',data[0]);
  //       pos = data[0]
  // })

    return (pos)
}
